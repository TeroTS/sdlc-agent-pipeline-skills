interface:
  display_name: "Implement Story"
  short_description: "Implement one selected backlog story"
  default_prompt: "Use $implement-story in implement mode with a story ID, or validation-failure mode with the same active story ID and structured validation results. Implement only that story and run its required checks."
